// Package middleware contains HTTP middleware used by the Gin router.
//
// Responsibilities:
// - Cross-cutting concerns (request IDs, auth hooks, logging helpers)
// - Keep handlers focused on transport concerns
package middleware
